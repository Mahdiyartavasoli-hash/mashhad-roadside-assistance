{
  "name": "mashhad-roadside-assistance",
  "version": "3.0.0",
  "private": true,
  "description": "Final frontend baseline for a mobile-first Mashhad roadside assistance website, built with HTML, Tailwind CSS and Vanilla JavaScript.",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./src/css/input.css -o ./src/css/output.css --watch",
    "build:css": "npx @tailwindcss/cli -i ./src/css/input.css -o ./src/css/output.css --minify",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
